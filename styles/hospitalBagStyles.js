import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 20,
        // backgroundColor: '#f7f7f7',
        flex: 1,
    },
    title: {
        textAlign: 'center',
        color: '#004d40',
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 0,
    },
    subtitle: {
        textAlign: 'center',
        marginTop: 4,
        marginBottom: 20,
        fontSize: 16,
        color: '#555',
    },
});
